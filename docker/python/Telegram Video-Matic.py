import streamlit

streamlit.title("Telegram Video-Matic")
streamlit.markdown("This is a Streamlit app connects to a Telegram chat, then will look for"
                   "un-downloaded videos and download them to a specified folder.")

streamlit.markdown("## Setup")
streamlit.markdown("1. Create a Telegram bot and get the API token.")
streamlit.markdown("2. Create a Telegram chat and add the bot as an admin.")
streamlit.markdown("3. Create a Telegram channel and add the chat as an admin.")
streamlit.markdown("4. Create a folder to store the downloaded videos.")
streamlit.markdown("5. Enter the required information below and click the 'Start' button.")


